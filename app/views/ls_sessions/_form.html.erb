<%= form_for(@ls_session) do |f| %>
  <% if @ls_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ls_session.errors.count, "error") %> prohibited this ls_session from being saved:</h2>

      <ul>
      <% @ls_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ls_parliament_id %><br />
    <%= f.number_field :ls_parliament_id %>
  </div>
  <div class="field">
    <%= f.label :session_number %><br />
    <%= f.number_field :session_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
